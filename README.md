# SnakeAI

To run this code run the file myagent. Ignore the trainingByBackprop accident that was put on this repository only the other three files are need for the code to run

Numpy is probably the only piece code of needed otherwise check the dependencies  via the code on what's needed